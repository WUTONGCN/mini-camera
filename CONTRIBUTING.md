# 贡献指南

提交前执行 `npm ci`、`npm run check`、`npm test`、`npm run build:dir`。
摄像头与窗口行为变更请注明操作系统、Electron 版本和实际设备测试范围。
不要提交配置、个人视频、抓拍、收款图片、证书或本地绝对路径。
请保持摄像头处理本地运行、音频关闭、IPC 白名单和无机器码授权的社区版行为。
提交的自有贡献按仓库 MIT 许可证提供；引入第三方内容时保留其原许可与来源。
